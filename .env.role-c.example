# Copy to .env.role-c.local. Never commit the local file or paste its key into logs/chat.
ROLE_C_PROVIDER_MODE=model
ROLE_C_MODEL_ENDPOINT=https://your-provider.example/v1/chat/completions
ROLE_C_MODEL_ID=your-model-id
ROLE_C_MODEL_API_KEY=

# Offline competition evaluation judge. These values are used only by
# scripts/competition-evaluation.ts and do not change the normal GLM runtime.
# DeepSeek example:
#   endpoint=https://api.deepseek.com/chat/completions
#   model=deepseek-v4-pro
# Existing GLM settings are loaded from this local runtime file when explicit
# generation variables are not set.
COMPETITION_GENERATION_CONFIG_PATH=.tmp/integrated-orchestrator/provider-config.json
COMPETITION_JUDGE_ENDPOINT=
COMPETITION_JUDGE_MODEL_ID=
COMPETITION_JUDGE_API_KEY=
COMPETITION_JUDGE_RESPONSE_FORMAT=json_object
COMPETITION_JUDGE_SCHEMA_STRICT=false
COMPETITION_JUDGE_THINKING=enabled
COMPETITION_JUDGE_TIMEOUT_MS=120000

# Project model runtime. MODEL_RUNTIME_* takes precedence; ROLE_C_MODEL_* remains
# as a compatibility connection alias for existing launch scripts.
MODEL_RUNTIME_MODEL_ID=
MODEL_RUNTIME_ENDPOINT=
MODEL_RUNTIME_API_KEY=
MODEL_RUNTIME_MAX_IN_FLIGHT=3
MODEL_RUNTIME_QUALITY_MAX_IN_FLIGHT=1
MODEL_RUNTIME_OFFLINE_MAX_IN_FLIGHT=1
MODEL_RUNTIME_JOB_SOFT_DEADLINE_MS=600000
MODEL_RUNTIME_JOB_HARD_DEADLINE_MS=900000
# 留空时按本轮目标数、题量和公开候选数自动计算。
MODEL_RUNTIME_MAX_MODEL_CALLS=
MODEL_RUNTIME_TRANSPORT_RETRY_BUDGET=
MODEL_RUNTIME_RESPONSE_FORMAT=json_object
MODEL_RUNTIME_FORCE_FAST=false
MODEL_CONFIG_READONLY=true

# json_schema: send the full schema; json_object: request JSON mode (DeepSeek uses this);
# text_json: prompt-only JSON.
ROLE_C_MODEL_RESPONSE_FORMAT=json_schema
# The current rich schemas use conditional/optional fields. Keep false unless the provider
# explicitly supports the full schemas in strict mode; local Ajv validation remains mandatory.
ROLE_C_MODEL_SCHEMA_STRICT=false
# Compatibility only. Production calls use the versioned FAST/QUALITY/OFFLINE_MAX policy.
ROLE_C_MODEL_THINKING=disabled

ROLE_C_MODEL_TIMEOUT_MS=120000
ROLE_C_MODEL_MAX_RETRIES=1
ROLE_C_MODEL_AUTH_HEADER=authorization
ROLE_C_MODEL_AUTH_SCHEME=Bearer

# Durable local D→C runs, secure artifacts, sessions and mastery state.
# Relative paths are resolved from the repository root; .tmp/ is Git-ignored.
ROLE_C_RUNTIME_DATA_DIR=.tmp/role-c-runtime

# staged is the normal path; monolithic is retained for compatibility/benchmark comparison.
ROLE_C_MODEL_GENERATION_STRATEGY=staged
ROLE_C_MODEL_MAX_REPAIR_ATTEMPTS=2
# Structured generation is most stable at 0; raise only after provider-specific evaluation.
ROLE_C_MODEL_CONCEPT_TEMPERATURE=0
ROLE_C_MODEL_CODE_LAB_TEMPERATURE=0
ROLE_C_MODEL_ASSESSMENT_TEMPERATURE=0

# Natural staged boundaries. The shared runtime limits all provider traffic to 3.
ROLE_C_MODEL_CONCEPT_GROUP_SIZE=1
ROLE_C_MODEL_CONCEPT_CONCURRENCY=2
ROLE_C_MODEL_CONCEPT_SEGMENT_MAX_TOKENS=12000
ROLE_C_MODEL_CODE_LAB_PUBLIC_MAX_TOKENS=16000
ROLE_C_MODEL_CODE_LAB_SECURE_MAX_TOKENS=16000
ROLE_C_MODEL_ASSESSMENT_PUBLIC_MAX_TOKENS=24000
ROLE_C_MODEL_ASSESSMENT_SECURE_MAX_TOKENS=16000

# Monolithic compatibility/benchmark budgets.
ROLE_C_MODEL_CONCEPT_MAX_TOKENS=24000
ROLE_C_MODEL_CODE_LAB_MAX_TOKENS=20000
ROLE_C_MODEL_ASSESSMENT_MAX_TOKENS=24000

# Role C code execution. Build the default image with:
# bun run docker:role-c:build
ROLE_C_DOCKER_BINARY=docker
ROLE_C_DOCKER_IMAGE=knowbalance-role-c-python-runner:1.0.0
ROLE_C_DOCKER_CPUS=0.5
ROLE_C_DOCKER_PIDS=32
ROLE_C_DOCKER_TMPFS_MB=16
